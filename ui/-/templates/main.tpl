<div class="{__NODE_ID__}" instance="{__INSTANCE__}">

    <div class="images">
        <!-- image -->
        <div class="image {HIDDEN_CLASS}" image_id="{ID}">
            <!-- if image/cp -->
            <div class="cp">
                <div class="l">

                </div>
                <div class="r">
                    <!-- image/not_published_mark -->
                    <div class="not_published_mark {HIDDEN_CLASS}">
                        <div class="fa fa-eye"></div>
                    </div>
                    <!-- / -->
                    {PAGE_DIALOG_BUTTON}
                </div>
            </div>
            <!-- / -->
            {CONTENT}
        </div>
        <!-- / -->
    </div>

</div>
