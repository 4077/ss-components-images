<div class="{__NODE_ID__} {ENABLED_CLASS}" instance="{__INSTANCE__}" hover="hover">

    <div class="left">
        {*<!-- image_instance -->*}
        <div class="image">{IMAGE}</div>
        {*<!-- / -->*}
        <div class="cache_size">
            {CACHE_SIZE}
        </div>
    </div>
    <div class="right">
        <div class="buttons">
            {TOGGLE_ENABLED_BUTTON}
            {DUPLICATE_BUTTON}
            {DELETE_BUTTON}
        </div>
        <div class="header" hover="hover">{HEADER}</div>
        <div class="link_cp">
            <div class="row">
                {LINK_ENABLED_BUTTON}
                {LINK_MODE_SWITCHER}
            </div>
            <!-- link_value -->
            <div class="row link_value">
                {CONTENT}
            </div>
            <!-- / -->
        </div>
        <div class="text" hover="hover">{TEXT}</div>
    </div>
    <div class="cb"></div>

</div>
